import 'package:flutter/material.dart';

class AllergensPage extends StatelessWidget {
  const AllergensPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
